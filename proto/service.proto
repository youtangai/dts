syntax="proto3";

message FileData {
  string filename = 1;
  int32 mode = 2;
  bytes data = 3;
}

message Response {
  int32 status = 1;
}

service FileTransferService {
  rpc FileTransfer(stream FileData) returns (Response) {}
}